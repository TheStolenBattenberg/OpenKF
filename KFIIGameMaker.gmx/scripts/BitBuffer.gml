///BitBuffer(buffer);

var arrBufferBits;

arrBufferBits[0] = argument0;   //Buffer
arrBufferBits[1] = 0;           //Current U16 bit buffer
arrBufferBits[2] = 16;          //Current bit position (0 - 7)

return arrBufferBits;
